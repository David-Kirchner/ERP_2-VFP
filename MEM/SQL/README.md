# SQL scripts moved

**SAUSA-ERP** = this VFP app + the SQL `SAUSA` database/scripts. The SQL tree no longer lives in this repo.

| | |
|---|---|
| **Local (OneDrive sync)** | `C:\Users\talkt\OneDrive - Space Alloys USA\SQL` |
| **GitHub** | https://github.com/David-Kirchner/SQL-ERP_2 |
| **Home / Development** | `SuperMicro` (`192.168.31.172`) · database `SAUSA` |
| **Production** | `BobWhite` (`192.168.5.3`) · database `SAUSA` |
| **Domain test (optional)** | `SA-WS-DK-26` (`192.168.5.88`) · database `SAUSA` |

See `SERVERS.md` in the SQL repo. Edit and commit SQL only there — this folder is a pointer only.
